{
  "description": "Should fail to create record with subfield containing subfield ending with whitespace if allowSubfieldValuesEndingWhitespace is false ",
  "skip": false,
  "noinput": true,
  "operations": [
    { "name": "MarcRecord", "args": {
      "validationOptions": {"allowSubfieldValuesEndingWhitespace": false},
      "leader": "foo",
      "fields": [
        {"tag": "500", "subfields": [{"code": "a", "value": "foo "}]}
      ]
    }}
  ],
  "throws": "Record is invalid",
  "immutable": true
}
